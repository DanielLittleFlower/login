name: Login
description: A simple login and registration page.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  http: ^0.13.3
  shared_preferences: ^2.0.10
  google_sign_in: ^6.0.2
  firebase_core: ^2.8.0
  firebase_auth: ^4.3.0
  flutter_signin_button: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/images/
